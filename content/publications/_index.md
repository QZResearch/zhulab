---
layout: "simple"   # plain page: no trailing article list
title: "Publications"
description: "Peer-reviewed output from the Zhu Group."
---

<!-- MAINTAINER NOTE (not shown on the page):
     Don't edit data/publications.yaml by hand — it is generated. Add the paper
     to publications.bib at the top of the repo, then run:

         python3 scripts/sync_publications.py

     Set `featured: true` on an entry in data/publications.yaml to also show it
     on the homepage.

     For the bolding below to cover everyone, add each member's published name
     (e.g. "Q. Zhu") to `groupAuthors` in config/_default/params.toml.

     See README.md. -->

Group members are shown in **bold**. Preprints and code are linked where they
exist; if you can't reach a paper, [email us](mailto:qiangzhu@uow.edu.au) and
we'll send a copy.

{{< publications >}}
